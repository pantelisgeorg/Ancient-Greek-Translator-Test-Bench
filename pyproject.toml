[project]
name = "greek-translator-test"
version = "0.1.0"
description = "Test bench for LLM-based Ancient Greek translation with editable system prompts"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.30",
    "openai>=1.40",
]

[tool.uv]
package = false
